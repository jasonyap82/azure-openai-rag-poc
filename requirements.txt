openai>=1.54.0
azure-identity>=1.19.0
azure-search-documents>=11.6.0
pydantic-settings>=2.6.0
tiktoken>=0.8.0
numpy>=1.26.0
tenacity>=9.0.0
python-dotenv>=1.0.1
rich>=13.9.0

# Verify TLS against the OS trust store rather than certifi's bundle. Needed on any
# machine where endpoint protection or a proxy re-signs HTTPS traffic.
truststore>=0.10.0

# Demo interface
streamlit>=1.40.0

# Document loaders (only needed for the formats you actually use)
pypdf>=5.1.0
python-docx>=1.1.2
